services:
  - type: web
    name: taaa-semantic-analyzer
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.4       # ✅ Use Python 3.12 instead of 3.13
    autoDeploy: true


